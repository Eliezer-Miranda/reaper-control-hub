# Default REAPER target (overridden by request headers from the frontend).
REAPER_HOST=192.168.2.104
REAPER_PORT=8080
REAPER_PASSWORD=

# Port that the proxy itself listens on (frontend points here).
PROXY_PORT=3001
